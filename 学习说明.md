# Claude Cookbooks 学习说明

## 1. 项目定位

这个项目是 Anthropic Claude Cookbooks 的示例集合，主要面向想用 Claude API、Claude Agent SDK、Managed Agents、工具调用、多模态、RAG、评测、Skills 等能力做应用开发的学习者和开发者。

它不是一个单一可运行的业务系统，而是一套“按主题组织的实践配方”。项目中的大部分内容是 Jupyter Notebook，每个 Notebook 聚焦一个场景，配套包含示例数据、辅助脚本、评测脚本或第三方服务集成代码。

适合用来：

- 快速理解 Claude API 的常见开发模式。
- 复制某个 Notebook 中的代码片段到自己的项目中改造。
- 学习 RAG、分类、摘要、Text-to-SQL、知识图谱等能力的端到端实现。
- 学习工具调用、Agent 编排、Claude Agent SDK、Managed Agents 的工程化用法。
- 参考项目中的评测脚本，给自己的提示词或 AI 工作流建立测试体系。

## 2. 项目结构速览

| 路径 | 内容 | 建议用途 |
| --- | --- | --- |
| `README.md` | 项目总览和官方配方目录 | 先读英文部分，当前仓库的中文翻译存在乱码 |
| `capabilities/` | Claude 基础能力专题：分类、RAG、摘要、Text-to-SQL、知识图谱、Contextual Embeddings | 推荐作为主线学习入口 |
| `tool_use/` | 工具调用示例：计算器、结构化 JSON、并行工具、工具搜索、记忆、上下文压缩等 | 学习如何让 Claude 调用外部函数和系统 |
| `multimodal/` | 视觉和文档理解：图片入门、视觉最佳实践、表单转录、图表/PPT 读取、裁剪工具 | 学习图像、PDF、图表等多模态输入 |
| `extended_thinking/` | Extended Thinking 与工具调用结合 | 学习复杂推理任务的处理方式 |
| `patterns/agents/` | Agent 工作流模式：基础工作流、评估优化器、编排-执行者模式 | 学习 Agent 架构模式 |
| `claude_agent_sdk/` | Claude Agent SDK 教程，从研究 Agent 到 SRE Agent、部署 Agent | 学习 SDK 方式构建通用 Agent |
| `managed_agents/` | Claude Managed Agents 托管运行时示例 | 学习有状态、可使用工具、可跨轮保存上下文的托管 Agent |
| `skills/` | Claude Skills 示例：文档生成、金融分析、自定义 Skill | 学习如何给 Claude 打包领域能力 |
| `third_party/` | 第三方集成：Pinecone、MongoDB、LlamaIndex、Wikipedia、ElevenLabs、Deepgram 等 | 有具体外部服务需求时再看 |
| `misc/` | 零散但实用的专题：JSON mode、评测、批处理、引用、PDF 摘要、Prompt caching 等 | 按问题查阅 |
| `coding/` | 前端审美提示词示例 | 做 UI/前端相关生成时参考 |
| `observability/` | 用量和成本 API 示例 | 需要监控 API 使用成本时参考 |
| `tool_evaluation/` | 工具评测示例 | 做工具调用质量评估时参考 |
| `tests/`、`scripts/` | Notebook 结构测试、校验脚本、作者校验脚本 | 贡献或批量验证示例时使用 |

## 3. 环境准备

项目配置要求 Python 版本为 `>=3.11,<3.13`，推荐使用 `uv` 管理依赖。

### 3.1 安装依赖

在项目根目录执行：

```powershell
uv sync --all-extras
```

或使用 Makefile：

```powershell
make install
```

如果本机没有 `make`，直接使用 `uv` 命令即可。

### 3.2 配置 API Key

根目录提供了 `.env.example`。建议复制为 `.env`，然后写入自己的 Anthropic API Key：

```text
ANTHROPIC_API_KEY=你的_api_key
```

部分 Notebook 会用到额外凭证，例如：

- `GITHUB_TOKEN`：Claude Agent SDK、Managed Agents 中涉及 GitHub 的示例可能需要。
- Pinecone、MongoDB、Deepgram、ElevenLabs、WolframAlpha 等第三方服务的 API Key：只在对应第三方目录中需要。

不要把 `.env` 提交到 Git。

### 3.3 启动 Jupyter

安装依赖后，在根目录执行：

```powershell
uv run jupyter notebook
```

或：

```powershell
uv run jupyter lab
```

然后打开对应目录下的 `.ipynb` 文件，按顺序运行单元格。

## 4. 推荐学习路线

### 路线 A：Claude API 基础能力入门

适合刚开始学习 Claude 应用开发的人。

1. `capabilities/summarization/guide.ipynb`
   先学摘要，因为它容易理解，也能看到长文本处理、分块、结果评估等常见模式。

2. `capabilities/classification/guide.ipynb`
   学分类任务，重点理解如何把业务规则、少量样本和评测结合起来。

3. `misc/how_to_enable_json_mode.ipynb`
   学结构化输出，后续做工具调用、数据抽取、系统集成都绕不开。

4. `misc/building_evals.ipynb`
   建议尽早看评测。提示词和 AI 工作流不能只靠感觉，需要可重复验证。

### 路线 B：RAG 与知识增强

适合想做企业知识库、问答系统、文档助手的人。

1. `capabilities/retrieval_augmented_generation/guide.ipynb`
   主线学习 RAG 的构建、优化和评测。

2. `capabilities/contextual-embeddings/guide.ipynb`
   学习如何给 chunk 补充上下文，改善传统 RAG 中“片段孤立”的问题。

3. `third_party/Pinecone/rag_using_pinecone.ipynb` 或 `third_party/MongoDB/rag_using_mongodb.ipynb`
   有具体向量数据库选型时再进入第三方实现。

4. `capabilities/knowledge_graph/guide.ipynb`
   当需要实体、关系、多跳查询时，再学习知识图谱方案。

### 路线 C：工具调用与 Agent

适合想做自动化助手、业务 Agent、DevOps Agent 的人。

1. `tool_use/calculator_tool.ipynb`
   从最小工具调用例子入门。

2. `tool_use/extracting_structured_json.ipynb`
   学结构化结果抽取和约束输出。

3. `tool_use/parallel_tools.ipynb`
   学并行调用多个工具，理解效率和状态管理问题。

4. `patterns/agents/basic_workflows.ipynb`
   学 Agent 工作流的基础模式。

5. `patterns/agents/orchestrator_workers.ipynb`
   学任务拆分、编排和 worker 分工。

6. `claude_agent_sdk/00_The_one_liner_research_agent.ipynb`
   进入 Claude Agent SDK，理解 `query()`、工具、会话上下文。

7. `claude_agent_sdk/01_The_chief_of_staff_agent.ipynb`
   学更完整的 Agent 结构：记忆、上下文、hooks、子 Agent。

### 路线 D：托管 Agent 与生产化

适合已经理解基础 Agent，想看更接近生产环境的运行方式的人。

1. `managed_agents/CMA_iterate_fix_failing_tests.ipynb`
   官方推荐入口，围绕“运行-观察-修复”循环介绍 agent、environment、session、文件挂载和流式事件。

2. `managed_agents/CMA_gate_human_in_the_loop.ipynb`
   学人类审批、工具回调、`requires_action` 处理。

3. `managed_agents/CMA_prompt_versioning_and_rollback.ipynb`
   学提示词版本管理、评测、回滚。

4. `managed_agents/CMA_operate_in_production.ipynb`
   学生产环境中的 MCP toolsets、vault、webhook、资源生命周期。

5. `managed_agents/sre_incident_responder.ipynb`
   看一个更完整的 on-call/SRE 工作流。

### 路线 E：多模态与文档处理

适合做 OCR、图片理解、表单抽取、图表分析、PDF 总结的人。

1. `multimodal/getting_started_with_vision.ipynb`
2. `multimodal/best_practices_for_vision.ipynb`
3. `multimodal/how_to_transcribe_text.ipynb`
4. `multimodal/reading_charts_graphs_powerpoints.ipynb`
5. `misc/pdf_upload_summarization.ipynb`

### 路线 F：Skills 与办公自动化

适合做 Excel、PPT、PDF、Word、金融报表、企业模板自动化的人。

1. `skills/notebooks/01_skills_introduction.ipynb`
2. `skills/notebooks/02_skills_financial_applications.ipynb`
3. `skills/notebooks/03_skills_custom_development.ipynb`

这一部分还包含 `skills/custom_skills/`，可以参考其中的 `SKILL.md` 和脚本结构，学习如何把领域知识、代码和资源打包成可复用 Skill。

## 5. 推荐使用方法

### 5.1 不要从头到尾机械刷完

这个仓库内容很多，最有效的方法是“按目标选配方”。建议先明确你要解决的问题：

- 想做知识库问答：看 RAG 路线。
- 想做业务自动化 Agent：看工具调用和 Agent 路线。
- 想做文档理解：看多模态路线。
- 想做办公文档生成：看 Skills 路线。
- 想把 AI 功能做可靠：尽早看 `misc/building_evals.ipynb` 和各 `evaluation/` 目录。

### 5.2 每个 Notebook 用三遍

第一遍只读 Markdown 和代码结构，理解它在解决什么问题。

第二遍运行单元格，确认依赖、凭证、输入输出都能跑通。

第三遍替换成自己的数据或业务规则，观察哪些 prompt、schema、工具定义、评测用例需要修改。

### 5.3 学习时重点记录这些东西

建议在自己的笔记里为每个 Notebook 记录：

- 这个示例解决的真实问题是什么。
- 输入是什么，输出是什么。
- Claude 的系统提示词或关键 prompt 是什么。
- 是否用了工具调用、结构化输出、RAG、缓存、评测。
- 哪些代码可以直接复用。
- 迁移到自己项目时，需要替换哪些数据、API Key、数据库或业务规则。

### 5.4 先做最小闭环，再追求完整系统

比如做一个企业知识库，不建议一开始就同时上向量库、重排序、知识图谱、权限、多 Agent。更稳的顺序是：

1. 用少量文档跑通最小 RAG。
2. 加入评测问题集。
3. 根据评测结果优化 chunk、检索、rerank、prompt。
4. 再接入真实向量库和权限体系。
5. 最后考虑 Agent 化、工具调用和生产监控。

## 6. 常用命令

### 安装依赖

```powershell
uv sync --all-extras
```

### 启动 Notebook

```powershell
uv run jupyter notebook
```

### 格式化代码

```powershell
uv run ruff format .
```

或：

```powershell
make format
```

### 代码检查

```powershell
uv run ruff check .
```

或：

```powershell
make lint
```

### 运行测试

```powershell
uv run pytest
```

或：

```powershell
make test
```

### 快速检查 Notebook 结构

```powershell
make test-notebooks
```

### 检查某一个 Notebook

```powershell
make test-notebooks NOTEBOOK=tool_use/calculator_tool.ipynb
```

### 执行 Notebook 测试

```powershell
make test-notebooks-exec NOTEBOOK=tool_use/calculator_tool.ipynb
```

注意：执行 Notebook 测试通常会真实调用 API，需要配置 `ANTHROPIC_API_KEY`，也可能产生费用。

## 7. 注意事项

- 当前仓库中多个 README 的中文翻译部分存在乱码，建议优先阅读英文内容。
- 很多 Notebook 会真实调用 Claude API，运行前确认 API Key、模型、token、费用预算。
- 第三方集成目录通常需要额外账号和 API Key，不需要时可以先跳过。
- 多模态 Notebook 中包含图片、PDF 等素材，文件较大，运行时注意 Notebook 加载速度。
- `managed_agents/`、`claude_agent_sdk/` 中部分示例依赖 Docker、Node.js、Claude Code CLI、GitHub Token 或外部服务。
- 评测脚本和慢速 Notebook 测试可能耗时较长，不建议一上来全量执行。
- 如果要把示例迁移到生产项目，务必补充错误处理、重试、日志、权限控制、成本控制和数据安全策略。

## 8. 建议的第一周学习安排

| 天数 | 学习内容 | 目标 |
| --- | --- | --- |
| Day 1 | 阅读根目录 `README.md` 英文部分，跑通环境和 Jupyter | 了解项目结构，确认 API Key 可用 |
| Day 2 | 学 `summarization` 和 `classification` | 掌握基础 prompt、样例、评测思路 |
| Day 3 | 学 `how_to_enable_json_mode` 和 `building_evals` | 建立结构化输出和质量验证意识 |
| Day 4 | 学 `retrieval_augmented_generation` | 跑通一个最小 RAG |
| Day 5 | 学 `calculator_tool`、`parallel_tools`、`basic_workflows` | 理解工具调用与 Agent 工作流 |
| Day 6 | 学 `getting_started_with_vision` 或 `skills` 入门 | 根据个人方向选择多模态或办公自动化 |
| Day 7 | 选一个自己的小场景改造 Notebook | 形成可复用的小 demo |

## 9. 最推荐先看的文件

如果只想快速抓住项目价值，建议按这个顺序打开：

1. `capabilities/README.md`
2. `capabilities/summarization/guide.ipynb`
3. `misc/how_to_enable_json_mode.ipynb`
4. `misc/building_evals.ipynb`
5. `capabilities/retrieval_augmented_generation/guide.ipynb`
6. `tool_use/calculator_tool.ipynb`
7. `patterns/agents/basic_workflows.ipynb`
8. `claude_agent_sdk/00_The_one_liner_research_agent.ipynb`

这条路线能覆盖 Claude 应用开发中最常见的核心能力：提示词、结构化输出、评测、RAG、工具调用和 Agent。
